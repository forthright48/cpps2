const {getUserInfo} = require('./lib/vjudgeScraper');

getUserInfo('labib666', 'cc')
    .then((data) => {
      console.log(JSON.stringify(data, null, 2));
    })
    .catch((err) => {
      console.log(err);
    });
